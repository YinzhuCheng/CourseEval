# Layer 1 — CourseEval application runtime (web + worker.py).
# Install this set on production hosts. It is NOT the student code sandbox; that is Layer 3 (runner/).
fastapi
uvicorn[standard]
sqlalchemy
jinja2
python-multipart
redis
rq
email-validator
itsdangerous
nbformat
pymupdf
markdown
bleach
